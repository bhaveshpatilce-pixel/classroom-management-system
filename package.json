{
  "name": "classroom-management-system",
  "version": "1.0.0",
  "description": "Full-stack Classroom Management System",
  "scripts": {
    "install": "cd backend && npm install",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
